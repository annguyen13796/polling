service: PL-vote

provider:
  name: aws
  runtime: nodejs18.x
  stage: dev
  tags:
    author: khoa.pham@zoi.tech
  region: eu-central-1
  iamRoleStatements:
    - Effect: 'Allow'
      Action:
        - dynamodb:Query
        - dynamodb:Scan
        - dynamodb:GetItem
        - dynamodb:PutItem
        - dynamodb:UpdateItem
        - dynamodb:DeleteItem
        - dynamodb:BatchWriteItem
      Resource:
        - arn:aws:dynamodb:eu-central-1:437822295963:table/khoa.pham_polling_draftVote
  apiGateway:
    restApiId: 24dp6xrmlb
    restApiRootResourceId: cq0b9cenbg

functions:
  getDraftAnswersForUser:
    handler: dist/index.handler
    events:
      - http:
          path: /votes/userResponses/{userResponseId}/draftAnswersForQuestions
          method: GET
          cors: true
  putDraftAnswersForQuestion:
    handler: dist/index.handler
    events:
      - http:
          path: /votes/userResponses/{userResponseId}/draftAnswersForQuestions/{questionId}
          method: PUT
          cors: true
  putGeneralVotingStatusOfUser:
    handler: dist/index.handler
    events:
      - http:
          path: /votes/userResponses/{userResponseId}
          method: PUT
          cors: true

plugins:
  - serverless-offline
